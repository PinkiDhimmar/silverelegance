<html>
<head>
<link href="/public/css/style.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
   
    .sidebar {
      height: 100vh;
      color: #000;
      margin-top: 100px;
    }
    .sidebar .nav-link {
      color: #000;
    }
    .sidebar .nav-link.active, .sidebar .nav-link:hover {
      color: #000;
      background-color: aliceblue;
      
    }
    .content-area {
      padding: 30px;
      margin-top: 100px;
    }
  </style>
  </head>
<body>
<div class="container-fluid">
  <div class="row">

    <!-- Sidebar -->
    <div class="col-md-3 sidebar p-3">
      <h4 class="mb-4">Welcome, <%= user.name %></h4>
      <div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist">
        <button class="nav-link active" id="dashboard-tab" data-bs-toggle="pill" data-bs-target="#dashboard" type="button">Dashboard</button>
        <button class="nav-link" id="orders-tab" data-bs-toggle="pill" data-bs-target="#orders" type="button">My Orders</button>
        <button class="nav-link" id="profile-tab" data-bs-toggle="pill" data-bs-target="#profile" type="button">Profile</button>
        <button class="nav-link" id="support-tab" data-bs-toggle="pill" data-bs-target="#support" type="button">Support</button>
        <a class="nav-link text-danger" href="/logout" style="text-align: center;">Logout</a>
      </div>
    </div>

    <!-- Content -->
    <div class="col-md-9 content-area">
      <div class="tab-content" id="v-pills-tab">
        <div class="tab-pane fade show active" id="dashboard">
          <h3>Dashboard</h3>
          <p>Welcome to your customer dashboard. Here you can view your activity and manage your account.</p>
        </div>

        <div class="tab-pane fade" id="orders">
          <h3>My Orders</h3>
          <p>List of your recent orders will go here.</p>
        </div>

        <div class="tab-pane fade" id="profile">
          <h3>Profile</h3>
          <p>My Profile<hr></p>
          <table class="table table-borderless">
      <tr>
        <th>Name:</th>
        <td><%= user.name %></td>
      </tr>
      <tr>
        <th>Email:</th>
        <td><%= user.email %></td>
      </tr>
      <tr>
        <th>Password:</th>
        <td><%= user.password %></td>
      </tr>
      <tr>
        <th>Member Since:</th>
        <td><%= user.created_at ? user.created_at.toISOString().slice(0,10) : 'N/A' %></td>
      </tr>
    </table>
          
        </div>

        <div class="tab-pane fade" id="support">
          <h3>Support</h3>
          <p>Contact our support team or view FAQs.</p>
        </div>
      </div>
    </div>

  </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>